{
    "name": "PyRLtools Ubuntu Development",
    "image": "pyrltools:ubuntu",
    "runArgs": [
        "--platform=linux/amd64",
        "-v", "${localWorkspaceFolder}:/pyrltools"
    ],
    "settings": { 
        // Put any VS Code settings here, e.g., to specify the terminal to use.
    },
    "extensions": [
        // List any VS Code extensions you want installed inside the container.
    ],
    "postCreateCommand": "echo 'Container is ready!'",
    "workspaceFolder": "/pyrltools",
    "remoteUser": "root" // or specify another user if you're not running as root
}
